[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "headwater"
version = "0.1.0"
description = "Relational database sampler for training relational transformers"
requires-python = ">=3.13"
dependencies = ["numpy"]

[tool.maturin]
features = ["python"]
